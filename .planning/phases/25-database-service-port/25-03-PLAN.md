---
phase: 25-database-service-port
plan: 03
type: execute
wave: 3
depends_on: ["25-02"]
files_modified:
  - backend/app/services/firestore_service.py
  - backend/tests/test_auth_enforcement.py
  - backend/tests/test_fetch_missing.py
  - backend/requirements.txt
autonomous: true
requirements: [DB-01, DB-06]
must_haves:
  truths:
    - "firestore_service.py is deleted from the codebase"
    - "firebase-admin has zero import sites in backend"
    - "All tests pass with mock targets pointing to db_service"
    - "No google.cloud.firestore imports anywhere in backend"
  artifacts:
    - path: "backend/tests/test_auth_enforcement.py"
      provides: "Tests with db_service mock targets"
      contains: "db_service"
    - path: "backend/tests/test_fetch_missing.py"
      provides: "Tests with db_service mock targets"
      contains: "db_service"
  key_links:
    - from: "backend/tests/test_auth_enforcement.py"
      to: "backend/app/services/db_service.py"
      via: "mock.patch targets"
      pattern: "app.services.db_service"
---

<objective>
Update test mock targets, delete firestore_service.py, remove firebase-admin dependency, and verify clean codebase.

Purpose: Final cleanup -- remove all Firestore artifacts so no code path can reach Firestore.
Output: Clean codebase with zero Firestore references and passing test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-database-service-port/25-RESEARCH.md
@.planning/phases/25-database-service-port/25-02-SUMMARY.md

@backend/tests/test_auth_enforcement.py
@backend/tests/test_fetch_missing.py
@backend/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update test mock targets and delete firestore_service.py</name>
  <files>
    backend/tests/test_auth_enforcement.py
    backend/tests/test_fetch_missing.py
    backend/app/services/firestore_service.py
  </files>
  <read_first>
    backend/tests/test_auth_enforcement.py
    backend/tests/test_fetch_missing.py
    backend/app/services/firestore_service.py
    backend/app/services/db_service.py
  </read_first>
  <action>
**1. test_auth_enforcement.py** (5 mock patches):
Update all `@mock.patch` / `mock.patch()` targets from `app.services.firestore_service.*` to `app.services.db_service.*`. Function names should match (e.g., `get_user_jobs`, `get_recent_jobs`, `get_job`, `delete_job`).

IMPORTANT: db_service functions take `db: AsyncSession` as first parameter while Firestore versions don't. The mock return values may need adjustment:
- Firestore returns dicts, db_service returns SQLAlchemy model instances
- Check each mock's return_value and adjust to match what db_service returns (model objects or None)
- If test expects dict returns, either: (a) change test expectations to model objects, or (b) use `AsyncMock` that returns model-like objects with the needed attributes

**2. test_fetch_missing.py** (3 mock patches):
Update mock targets from `app.services.firestore_service.lookup_rrc_acres` and `lookup_rrc_by_lease_number` to `app.services.db_service.*`.

Note: `lookup_rrc_acres` now returns a dict (same shape as Firestore version per Plan 01), so mock return values should remain dicts -- no change needed for return values.

Also check if tests mock at the import site (e.g., `app.services.proration.csv_processor.lookup_rrc_acres`) rather than the definition site. Update accordingly based on where the function is imported.

**3. Delete firestore_service.py:**
After test updates, delete `backend/app/services/firestore_service.py`.

**4. Verify zero Firestore imports remain:**
Run `grep -r "firestore_service\|from google.cloud import firestore\|from google.cloud.firestore\|firebase_admin" backend/ --include="*.py"` and confirm empty output.

If any stray imports found, fix them.
  </action>
  <verify>
    <automated>cd /Users/yojimbo/Documents/dev/toolbox && test ! -f backend/app/services/firestore_service.py && echo "firestore_service.py deleted" && grep -r "firestore_service\|from google.cloud import firestore\|from google.cloud.firestore_v1\|firebase_admin" backend/app/ backend/tests/ --include="*.py"; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f backend/app/services/firestore_service.py` succeeds (file deleted)
    - `grep -r "firestore_service" backend/ --include="*.py"` returns empty (exit 1)
    - `grep -r "firebase_admin" backend/app/ --include="*.py"` returns empty (exit 1)
    - `grep "db_service" backend/tests/test_auth_enforcement.py` shows updated mock targets
    - `grep "db_service" backend/tests/test_fetch_missing.py` shows updated mock targets
  </acceptance_criteria>
  <done>firestore_service.py deleted, all test mocks updated to db_service targets, zero Firestore imports in codebase</done>
</task>

<task type="auto">
  <name>Task 2: Remove firebase-admin from requirements.txt and run full test suite</name>
  <files>
    backend/requirements.txt
  </files>
  <read_first>
    backend/requirements.txt
  </read_first>
  <action>
**1. Remove Firestore/Firebase dependencies from requirements.txt:**
Remove these lines (exact package names may vary):
- `firebase-admin` (or `firebase-admin>=...`)
- `google-cloud-firestore` (if listed separately from firebase-admin)

Do NOT remove `google-cloud-storage` yet (that's Phase 27).
Do NOT remove `google-genai` yet (that's Phase 26).

**2. Run full test suite to confirm everything passes:**
`cd backend && pytest -v`

**3. Final codebase audit:**
Run these checks and fix any remaining issues:
- `grep -r "firestore" backend/ --include="*.py" -l` -- should return empty (no files mention firestore at all)
- `grep -r "firebase_admin" backend/ --include="*.py" -l` -- should return empty
- `grep -r "from google.cloud import firestore" backend/ --include="*.py"` -- should return empty
- `grep -r "google.cloud.firestore" backend/ --include="*.py"` -- should return empty

If the auth module (`core/auth.py`) still references firebase_admin for anything, that should have been removed in Phase 23/24. Verify and fix if needed.
  </action>
  <verify>
    <automated>cd /Users/yojimbo/Documents/dev/toolbox/backend && grep -c "firebase-admin" requirements.txt; echo "EXIT:$?" && pytest -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "firebase-admin" backend/requirements.txt` returns empty (exit 1)
    - `grep -r "firestore" backend/ --include="*.py" -l` returns empty
    - `grep -r "firebase_admin" backend/ --include="*.py" -l` returns empty
    - `cd backend && pytest -v` exits with code 0 (all tests pass)
  </acceptance_criteria>
  <done>firebase-admin removed from requirements.txt, zero Firestore/Firebase references in entire backend, full test suite green</done>
</task>

</tasks>

<verification>
Phase-level verification (all DB-01, DB-05, DB-06 requirements):

1. DB-01: `grep -r "firestore_service\|get_firestore_client\|from google.cloud import firestore" backend/app/ --include="*.py"` returns empty
2. DB-05: `cd backend && python3 -c "from app.services import db_service; print(dir(db_service))"` shows all functions
3. DB-06: `test ! -f backend/app/services/firestore_service.py` succeeds AND `grep -r "firebase_admin" backend/ --include="*.py"` returns empty
4. All tests: `cd backend && pytest -v` passes
</verification>

<success_criteria>
- firestore_service.py deleted
- firebase-admin removed from requirements.txt
- Zero Firestore/Firebase imports anywhere in backend
- Full test suite passes with PostgreSQL as sole database
</success_criteria>

<output>
After completion, create `.planning/phases/25-database-service-port/25-03-SUMMARY.md`
</output>
