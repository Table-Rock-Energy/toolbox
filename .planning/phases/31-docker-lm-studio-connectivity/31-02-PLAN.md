---
phase: 31-docker-lm-studio-connectivity
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified: []
autonomous: false
requirements: [DOCKER-03]

must_haves:
  truths:
    - "User can upload a file, click Enrich, and receive AI-processed results on the server"
    - "Backend container successfully connects to LM Studio through host.docker.internal"
    - "Model verification passes before inference begins"
  artifacts: []
  key_links:
    - from: "frontend (Enrich button)"
      to: "/api/pipeline/cleanup"
      via: "POST request with tool + entries"
      pattern: "pipeline/cleanup"
    - from: "/api/pipeline/cleanup"
      to: "OpenAIProvider.cleanup_entries"
      via: "LLM provider factory"
      pattern: "get_llm_provider"
    - from: "OpenAIProvider"
      to: "LM Studio at host.docker.internal:1234"
      via: "AsyncOpenAI client + verify_model check"
      pattern: "llm_api_base"
---

<objective>
Verify the full AI enrichment pipeline works end-to-end on the server with Docker + LM Studio.

Purpose: DOCKER-03 requires confirming the complete user flow: upload a file, click Enrich, and receive AI-processed results. This is a human-verify checkpoint because it requires LM Studio running on the host and real inference.

Output: Confirmed E2E pipeline or identified issues to fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-docker-lm-studio-connectivity/31-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify E2E AI enrichment pipeline on server</name>
  <files>docker-compose.yml, backend/app/services/llm/openai_provider.py</files>
  <read_first>
    - docker-compose.yml
    - backend/app/services/llm/openai_provider.py
  </read_first>
  <action>
This is a human-verify checkpoint. Present the verification steps to the user and wait for confirmation.

Before presenting, run a quick sanity check:
1. Confirm docker-compose.yml has `extra_hosts` with `host.docker.internal:host-gateway`
2. Confirm openai_provider.py has `verify_model` method
3. Confirm all tests pass: `cd backend && python3 -m pytest tests/test_llm_protocol.py -x -q`

Then present the how-to-verify steps below for the user to execute on the server.
  </action>
  <what-built>
Docker Compose is configured with `extra_hosts` for host.docker.internal DNS resolution, AI environment variables (AI_PROVIDER, LLM_API_BASE, LLM_MODEL, LLM_MODELS_DIR), and a read-only volume mount for the LM Studio models directory. OpenAIProvider now verifies the configured model exists in LM Studio before making inference calls.
  </what-built>
  <how-to-verify>
Prerequisites: LM Studio must be running on the host with a model loaded.

1. SSH into the server
2. `cd /path/to/toolbox && docker-compose up -d --build`
3. Verify backend starts: `docker-compose logs backend | grep "Application startup complete"`
4. Verify LM Studio connectivity: `docker-compose exec backend python3 -c "import httpx; r = httpx.get('http://host.docker.internal:1234/v1/models'); print(r.json())"`
5. Open the app in browser, navigate to Extract (or any tool)
6. Upload a test file (e.g., a small OCC Exhibit A PDF)
7. Click "Enrich" / "AI Cleanup" button
8. Confirm: AI processing starts (progress shown), results appear, no 404/connection errors in backend logs
9. Check backend logs for model verification: `docker-compose logs backend | grep "Model verification\|verify_model"`
  </how-to-verify>
  <verify>
    <automated>grep -q "host.docker.internal:host-gateway" docker-compose.yml && grep -q "verify_model" backend/app/services/llm/openai_provider.py && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose exec backend python3 -c "import httpx; r = httpx.get('http://host.docker.internal:1234/v1/models'); print(r.status_code)"` prints 200
    - AI enrichment returns proposed changes (not an empty error response)
    - Backend logs show no `ConnectError` or model verification failures
  </acceptance_criteria>
  <done>User confirmed AI enrichment works end-to-end on the server: upload file, click Enrich, receive AI-processed results through Docker container connecting to host LM Studio.</done>
  <resume-signal>Type "approved" if enrichment works end-to-end, or describe the issue observed</resume-signal>
</task>

</tasks>

<verification>
- Full enrichment pipeline: upload -> enrich -> AI results displayed
- No connection errors in backend logs
- Model verification succeeds on first inference call
</verification>

<success_criteria>
- User confirmed AI enrichment works end-to-end on the server through Docker
</success_criteria>

<output>
After completion, create `.planning/phases/31-docker-lm-studio-connectivity/31-02-SUMMARY.md`
</output>
