---
phase: 27-storage-dependency-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/storage_service.py
  - backend/app/core/config.py
  - backend/requirements.txt
  - backend/app/api/admin.py
  - backend/tests/test_auth_enforcement.py
  - backend/tests/test_pipeline.py
  - frontend/src/pages/AdminSettings.tsx
  - frontend/src/contexts/OperationContext.tsx
  - .github/workflows/deploy.yml
autonomous: true
requirements: [STOR-01, STOR-02, CLEAN-01]

must_haves:
  truths:
    - "App starts with zero GCS warnings or errors in logs"
    - "All five tools process uploads and exports using local filesystem"
    - "requirements.txt has zero google-* dependencies"
    - "GitHub Actions deploy workflow is disabled"
  artifacts:
    - path: "backend/app/services/storage_service.py"
      provides: "Local-only storage service"
      contains: "class StorageService"
    - path: "backend/app/core/config.py"
      provides: "Config with no GCS defaults"
    - path: "backend/requirements.txt"
      provides: "Clean dependency list"
  key_links:
    - from: "backend/app/services/storage_service.py"
      to: "backend/app/core/config.py"
      via: "settings.data_dir"
      pattern: "settings\\.data_dir"
    - from: "backend/app/api/admin.py"
      to: "backend/app/services/storage_service.py"
      via: "profile_storage, storage_service imports"
      pattern: "from app.services.storage_service import"
---

<objective>
Remove all GCS code paths, google-cloud-storage dependency, and Cloud Run CI/CD. Make local filesystem the only storage backend. Rename admin settings endpoint from "google-cloud" to "api-settings" since it now manages AI/Maps/batch config with no GCS involvement.

Purpose: Eliminate all Google Cloud Storage dependency so the app runs fully on-prem with local filesystem.
Output: Local-only storage_service.py, clean requirements.txt, disabled deploy workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/services/storage_service.py
@backend/app/core/config.py
@backend/requirements.txt
@backend/app/api/admin.py
@.github/workflows/deploy.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip GCS from storage_service.py, config.py, and requirements.txt</name>
  <files>
    backend/app/services/storage_service.py
    backend/app/core/config.py
    backend/requirements.txt
  </files>
  <read_first>
    backend/app/services/storage_service.py
    backend/app/core/config.py
    backend/requirements.txt
    backend/app/api/admin.py (lines 440-520 for google-cloud settings)
    backend/app/services/proration/rrc_data_service.py (imports storage_service)
  </read_first>
  <action>
**storage_service.py** -- Complete rewrite to local-only:
- Remove the entire `try/except ImportError` block for `google.cloud import storage` (lines 15-21)
- Remove `GCS_AVAILABLE` variable
- Remove `_init_client()` method entirely
- Remove `is_gcs_enabled` property entirely
- Remove ALL GCS methods: `_upload_to_gcs`, `_download_from_gcs`, `_exists_in_gcs`, `_get_gcs_file_info`, `_delete_from_gcs`, `_list_gcs_files`
- Remove `get_signed_url()` method (always returned None for local anyway)
- Simplify each public method (`upload_file`, `download_file`, `file_exists`, `get_file_info`, `delete_file`, `list_files`) to call only the local `_*` methods directly -- no GCS branching
- Remove `self._client`, `self._bucket`, `self._initialized` from `__init__`
- Update class docstring to "Local filesystem storage service"
- In `RRCDataStorage.get_status()`, change `"storage_type"` value from conditional to hardcoded `"local"`
- Keep all helper classes (RRCDataStorage, UploadStorage, ProfileStorage) and global instances unchanged
- Rename `gcs_rrc_data_folder` references to just use the folder string. In config, rename fields: `gcs_rrc_data_folder` -> `storage_rrc_data_folder`, `gcs_uploads_folder` -> `storage_uploads_folder`, `gcs_profiles_folder` -> `storage_profiles_folder`. Update all references in storage_service.py and admin.py.

**config.py:**
- Remove `gcs_bucket_name` field entirely
- Remove `gcs_project_id` field entirely
- Rename `gcs_rrc_data_folder` to `storage_rrc_data_folder` (keep default "rrc-data")
- Rename `gcs_uploads_folder` to `storage_uploads_folder` (keep default "uploads")
- Rename `gcs_profiles_folder` to `storage_profiles_folder` (keep default "profiles")
- Remove `use_gcs` property entirely
- Remove comment "Google Cloud Storage settings" section header

**requirements.txt:**
- Remove `google-cloud-storage>=2.14.0` line
- Remove the comment `# Google Cloud Storage (persistent file storage)` above it
  </action>
  <verify>
    <automated>cd /Users/yojimbo/Documents/dev/toolbox && grep -c "google.cloud\|google-cloud-storage\|gcs_bucket\|use_gcs\|is_gcs_enabled\|GCS_AVAILABLE" backend/app/services/storage_service.py backend/app/core/config.py backend/requirements.txt | grep -v ":0$" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
- `grep -r "google.cloud" backend/app/services/storage_service.py` returns nothing
- `grep "google-cloud-storage" backend/requirements.txt` returns nothing
- `grep "gcs_bucket_name\|gcs_project_id\|use_gcs" backend/app/core/config.py` returns nothing
- `grep "gcs_rrc_data_folder\|gcs_uploads_folder\|gcs_profiles_folder" backend/app/core/config.py` returns nothing (renamed to storage_*)
- `python3 -c "from app.services.storage_service import storage_service"` succeeds (from backend dir)
  </acceptance_criteria>
  <done>storage_service.py is local-only with no GCS imports, config.py has no GCS fields, requirements.txt has no google packages</done>
</task>

<task type="auto">
  <name>Task 2: Update all consumers, rename admin endpoint, disable CI/CD</name>
  <files>
    backend/app/api/admin.py
    backend/tests/test_auth_enforcement.py
    backend/tests/test_pipeline.py
    frontend/src/pages/AdminSettings.tsx
    frontend/src/contexts/OperationContext.tsx
    .github/workflows/deploy.yml
  </files>
  <read_first>
    backend/app/api/admin.py (full file -- need GoogleCloudSettings* models and endpoint)
    backend/tests/test_auth_enforcement.py (line ~203 for google-cloud test)
    backend/tests/test_pipeline.py (line ~500 for GoogleCloudSettingsResponse test)
    frontend/src/pages/AdminSettings.tsx (lines with settings/google-cloud)
    frontend/src/contexts/OperationContext.tsx (line ~83 for settings/google-cloud)
    backend/app/services/proration/rrc_data_service.py (check for gcs_* config references)
    backend/app/api/admin.py (line ~650 for gcs_profiles_folder reference)
  </read_first>
  <action>
**admin.py:**
- Rename `GoogleCloudSettingsRequest` to `ApiSettingsRequest`
- Rename `GoogleCloudSettingsResponse` to `ApiSettingsResponse`
- Rename endpoint paths: `/settings/google-cloud` -> `/settings/api-config` (both GET and PUT)
- Update endpoint function names: `get_google_cloud_settings` -> `get_api_settings`, `update_google_cloud_settings` -> `update_api_settings`
- Update docstrings to remove "Google Cloud" references
- Update `settings.gcs_profiles_folder` reference (line ~650) to `settings.storage_profiles_folder`

**test_auth_enforcement.py:**
- Update line ~203: change `/api/admin/settings/google-cloud` to `/api/admin/settings/api-config`

**test_pipeline.py:**
- Update line ~500: change `GoogleCloudSettingsResponse` import to `ApiSettingsResponse`
- Update variable name and test description

**AdminSettings.tsx:**
- Change fetch URLs from `admin/settings/google-cloud` to `admin/settings/api-config`

**OperationContext.tsx:**
- Change fetch URL from `/api/admin/settings/google-cloud` to `/api/admin/settings/api-config`

**.github/workflows/deploy.yml:**
- Rename file to `.github/workflows/deploy.yml.disabled`
  </action>
  <verify>
    <automated>cd /Users/yojimbo/Documents/dev/toolbox && grep -rn "google-cloud\|GoogleCloud\|gcs_profiles_folder\|gcs_uploads_folder\|gcs_rrc_data_folder" backend/app/api/admin.py backend/tests/ frontend/src/ 2>/dev/null | grep -v node_modules | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
- `grep -r "settings/google-cloud" frontend/src/ backend/` returns nothing
- `grep -r "GoogleCloudSettings" backend/` returns nothing
- `test -f .github/workflows/deploy.yml` returns false (file renamed)
- `test -f .github/workflows/deploy.yml.disabled` returns true
- `grep "gcs_profiles_folder" backend/app/api/admin.py` returns nothing
  </acceptance_criteria>
  <done>All consumers updated to new endpoint names and storage_* config fields. CI/CD workflow disabled. Zero "google-cloud" string references in backend or frontend code.</done>
</task>

</tasks>

<verification>
```bash
# No google-cloud-storage anywhere in backend
grep -r "google.cloud\|google-cloud-storage" backend/ --include="*.py" --include="*.txt" | grep -v __pycache__ | grep -v ".pyc"

# No GCS config references
grep -r "gcs_bucket\|gcs_project_id\|use_gcs\|GCS_AVAILABLE\|is_gcs_enabled" backend/ --include="*.py" | grep -v __pycache__

# No google-cloud in frontend
grep -r "google-cloud" frontend/src/

# Deploy workflow disabled
test ! -f .github/workflows/deploy.yml && echo "deploy.yml removed" || echo "FAIL: deploy.yml still exists"

# Python syntax check
cd backend && python3 -c "from app.services.storage_service import storage_service, rrc_storage, upload_storage, profile_storage; print('OK')"

# TypeScript build check
cd frontend && npx tsc --noEmit
```
</verification>

<success_criteria>
- Local filesystem is the only storage backend -- no GCS code in codebase
- requirements.txt has zero google-* packages
- All five tools work with local filesystem storage
- CI/CD deploy workflow disabled
- Admin settings endpoint renamed from google-cloud to api-config
</success_criteria>

<output>
After completion, create `.planning/phases/27-storage-dependency-cleanup/27-01-SUMMARY.md`
</output>
