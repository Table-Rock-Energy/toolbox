---
phase: 01-auth-enforcement-and-cors-lockdown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/config.py
  - backend/app/core/auth.py
  - backend/app/main.py
  - backend/app/api/ghl.py
  - backend/pytest.ini
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_auth_enforcement.py
  - backend/tests/test_cors.py
autonomous: true
requirements: [AUTH-01, SEC-01]

must_haves:
  truths:
    - "Unauthenticated requests to any tool endpoint return 401"
    - "CORS rejects origins not in the allowlist"
    - "Dev mode (no Firebase SDK) accepts any Bearer token and returns synthetic user"
    - "SSE progress endpoint authenticates via query parameter token"
    - "Admin check endpoint remains unauthenticated"
    - "Health check endpoint remains unauthenticated"
  artifacts:
    - path: "backend/app/core/config.py"
      provides: "environment and cors_allowed_origins settings with cors_origins property"
      contains: "cors_origins"
    - path: "backend/app/core/auth.py"
      provides: "Dev-mode bypass returning synthetic user dict"
      contains: "dev-mode"
    - path: "backend/app/main.py"
      provides: "Router-level auth dependencies and config-driven CORS"
      contains: "dependencies="
    - path: "backend/app/api/ghl.py"
      provides: "SSE endpoint with query-param token auth"
      contains: "token: Optional[str]"
    - path: "backend/tests/conftest.py"
      provides: "Test fixtures for auth mocking"
      contains: "dependency_overrides"
    - path: "backend/tests/test_auth_enforcement.py"
      provides: "Auth enforcement tests"
      contains: "test_unauthenticated"
    - path: "backend/tests/test_cors.py"
      provides: "CORS configuration tests"
      contains: "test_cors"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/core/auth.py"
      via: "dependencies=[Depends(require_auth)]"
      pattern: "dependencies=\\[Depends\\(require_auth\\)\\]"
    - from: "backend/app/main.py"
      to: "backend/app/core/config.py"
      via: "settings.cors_origins"
      pattern: "settings\\.cors_origins"
    - from: "backend/app/core/auth.py"
      to: "backend/app/core/auth.py"
      via: "get_firebase_app() check for dev-mode bypass"
      pattern: "get_firebase_app.*None"
---

<objective>
Enforce authentication on all backend API endpoints (except health check and admin user check), replace wildcard CORS with config-driven origin allowlist, add dev-mode auth bypass, and add query-param auth to the SSE progress endpoint. Create test infrastructure to verify auth enforcement and CORS behavior.

Purpose: Close the security gap where tool endpoints are accessible without authentication, and fix the CORS spec violation (wildcard + credentials).
Output: Hardened backend with auth on all tool routes, proper CORS, SSE auth, dev-mode bypass, and passing test suite.
</objective>

<execution_context>
@/Users/yojimbo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yojimbo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-enforcement-and-cors-lockdown/01-CONTEXT.md
@.planning/phases/01-auth-enforcement-and-cors-lockdown/01-RESEARCH.md
@.planning/phases/01-auth-enforcement-and-cors-lockdown/01-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/app/core/auth.py:
```python
security = HTTPBearer(auto_error=False)

async def verify_firebase_token(token: str) -> Optional[dict]:
    # Returns None when Firebase not configured OR token invalid

def get_firebase_app():
    # Returns None when Firebase Admin SDK not initialized

async def get_current_user(request, credentials) -> Optional[dict]:
    # Returns user dict or None

async def require_auth(user: Optional[dict] = Depends(get_current_user)) -> dict:
    # Raises 401 if user is None

async def require_admin(user: dict = Depends(require_auth)) -> dict:
    # Chains on require_auth, checks admin role
```

From backend/app/core/config.py:
```python
class Settings(BaseSettings):
    # No `environment` or `cors_allowed_origins` fields yet
    # Has @property pattern for use_gcs, use_database, etc.
```

From backend/app/main.py (lines 48-81):
```python
# Current CORS — wildcard, needs replacement:
app.add_middleware(CORSMiddleware, allow_origins=["*"], allow_credentials=True, ...)

# Current router mounts — no auth dependencies:
app.include_router(extract_router, prefix="/api/extract", tags=["extract"])
# ... same pattern for all routers
```

From backend/app/api/ghl.py (line 391-392):
```python
@router.get("/send/{job_id}/progress")
async def stream_send_progress(job_id: str, request: Request):
    # Currently no auth — needs query-param token
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend auth enforcement, CORS lockdown, dev-mode bypass, SSE auth</name>
  <files>backend/app/core/config.py, backend/app/core/auth.py, backend/app/main.py, backend/app/api/ghl.py</files>
  <action>
**config.py** — Add two new fields to the Settings class:
- `environment: str = "development"` (reads from ENVIRONMENT env var)
- `cors_allowed_origins: str = ""` (reads from CORS_ALLOWED_ORIGINS env var, comma-separated)
- Add `@property def cors_origins(self) -> list[str]:` that returns:
  - Parsed comma-separated list if `cors_allowed_origins` is non-empty
  - `["https://tools.tablerocktx.com"]` if `environment == "production"`
  - `["http://localhost:5173"]` otherwise (development default)

**auth.py** — Fix `get_current_user` (around line 308) to handle dev-mode bypass:
- When `credentials` is not None but `verify_firebase_token` returns None:
  - Check `get_firebase_app()`. If it returns None (Firebase not configured = dev mode), log a warning and return a synthetic user dict `{"email": "dev@localhost", "uid": "dev-mode"}` instead of returning None.
  - If `get_firebase_app()` returns a real app (Firebase IS configured but token is invalid), return None as before (which will trigger 401 via `require_auth`).

**main.py** — Two changes:
1. Replace CORS middleware config (lines 48-55):
   - Import `settings` (already imported)
   - Use `allow_origins=settings.cors_origins` instead of `allow_origins=["*"]`
   - Keep `allow_credentials=True`
   - Use `allow_methods=["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"]` instead of `["*"]`
   - Use `allow_headers=["Authorization", "Content-Type"]` instead of `["*"]`

2. Add router-level auth dependencies (lines 70-81):
   - Add `from fastapi import Depends` and `from app.core.auth import require_auth` imports
   - Add `dependencies=[Depends(require_auth)]` to these router mounts: extract, title, proration, revenue, ghl_prep, history, ai, enrichment, etl
   - Do NOT add router-level auth to ghl_router (already has per-endpoint auth; SSE endpoint needs special handling)
   - Do NOT add router-level auth to admin_router (check_user endpoint must remain unauthenticated per user decision; mutations already use require_admin individually)

**ghl.py** — Add query-param auth to SSE endpoint (line 391-392):
- Change signature to: `async def stream_send_progress(job_id: str, request: Request, token: Optional[str] = None):`
- Add auth check before SSE logic:
  - If `token` is provided: call `verify_firebase_token(token)`. If it returns None AND `get_firebase_app()` is not None (Firebase configured but token invalid), raise 401.
  - If no `token` AND `get_firebase_app()` is not None (Firebase configured, production): raise 401.
  - If no `token` AND `get_firebase_app()` is None (dev mode): allow (no auth needed in dev)
- Add necessary imports: `from app.core.auth import verify_firebase_token, get_firebase_app` and `from typing import Optional`
  </action>
  <verify>
    <automated>cd /Users/yojimbo/Documents/dev/toolbox && python3 -c "from backend.app.core.config import settings; print('cors_origins:', settings.cors_origins); print('environment:', settings.environment)" && grep -q "settings.cors_origins" backend/app/main.py && grep -q "dependencies=\[Depends(require_auth)\]" backend/app/main.py && echo "All checks passed"</automated>
  </verify>
  <done>
- config.py has environment + cors_allowed_origins fields with cors_origins property
- auth.py get_current_user returns synthetic user dict in dev mode when Firebase not configured
- main.py uses settings.cors_origins for CORS and has dependencies=[Depends(require_auth)] on all tool routers
- ghl.py SSE endpoint accepts ?token= query parameter for auth
- Admin router and GHL router do NOT have router-level auth
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test infrastructure and auth/CORS tests</name>
  <files>backend/pytest.ini, backend/tests/__init__.py, backend/tests/conftest.py, backend/tests/test_auth_enforcement.py, backend/tests/test_cors.py</files>
  <behavior>
    - test_unauthenticated_extract_returns_401: GET /api/extract/health without token returns 401
    - test_unauthenticated_title_returns_401: GET /api/title/health without token returns 401
    - test_unauthenticated_proration_returns_401: GET /api/proration/rrc/status without token returns 401
    - test_unauthenticated_revenue_returns_401: POST /api/revenue/upload without token returns 401
    - test_unauthenticated_history_returns_401: GET /api/history/jobs without token returns 401
    - test_authenticated_health_returns_200: GET /api/health (no auth) returns 200
    - test_authenticated_extract_succeeds: GET /api/extract/health with auth override does not return 401
    - test_admin_check_no_auth_required: GET /api/admin/users/test@example.com/check without token does not return 401
    - test_dev_mode_bypass: When Firebase not configured, request with Bearer token gets synthetic user (not 401)
    - test_cors_rejects_unknown_origin: Request with Origin: https://evil.com gets no Access-Control-Allow-Origin header
    - test_cors_allows_configured_origin: Request with Origin: http://localhost:5173 gets proper CORS headers
  </behavior>
  <action>
**pytest.ini** — Create with:
```ini
[pytest]
asyncio_mode = auto
testpaths = tests
```

**tests/__init__.py** — Empty file.

**tests/conftest.py** — Create shared fixtures:
- `mock_user` fixture returning `{"email": "test@example.com", "uid": "test-uid"}`
- `authenticated_client` async fixture: set `app.dependency_overrides[require_auth]` to return mock_user, yield `AsyncClient(transport=ASGITransport(app=app), base_url="http://test")`, then clear overrides in teardown
- `unauthenticated_client` async fixture: no overrides, yield `AsyncClient(transport=ASGITransport(app=app), base_url="http://test")`, clear overrides in teardown
- Import `require_auth` from `app.core.auth`, `app` from `app.main`
- Use `pytest_asyncio.fixture` for async fixtures

**tests/test_auth_enforcement.py** — Write tests covering AUTH-01:
- Test unauthenticated requests to representative endpoints from each protected router return 401 (extract, title, proration, revenue, ghl-prep, history, ai, enrichment, etl — pick one endpoint per router)
- Test GET /api/health returns 200 without auth
- Test GET /api/admin/users/test@example.com/check does NOT return 401 without auth
- Test authenticated requests to a tool endpoint succeed (do not return 401)

**tests/test_cors.py** — Write tests covering SEC-01:
- Test that a request with `Origin: https://evil.com` to `/api/health` does NOT get `Access-Control-Allow-Origin` in the response
- Test that a request with `Origin: http://localhost:5173` to `/api/health` gets `Access-Control-Allow-Origin: http://localhost:5173` in the response
- Test CORS preflight (OPTIONS) with allowed origin returns proper headers
  </action>
  <verify>
    <automated>cd /Users/yojimbo/Documents/dev/toolbox/backend && python3 -m pytest tests/ -v</automated>
  </verify>
  <done>
- pytest.ini exists with asyncio_mode = auto
- tests/conftest.py has authenticated_client and unauthenticated_client fixtures
- All auth enforcement tests pass (unauthenticated = 401, authenticated = not 401, health = 200, admin check = not 401)
- All CORS tests pass (unknown origin rejected, known origin allowed)
  </done>
</task>

</tasks>

<verification>
1. `cd backend && python3 -m pytest tests/ -v` — all tests pass
2. `python3 -c "from app.core.config import settings; assert hasattr(settings, 'cors_origins')"` — config has cors_origins
3. Grep for `allow_origins=["*"]` in main.py — should NOT be found
4. Grep for `dependencies=` in main.py — should show require_auth on tool routers
</verification>

<success_criteria>
- All tool endpoints (extract, title, proration, revenue, ghl-prep, history, ai, enrichment, etl) return 401 without auth token
- GET /api/health returns 200 without auth
- GET /api/admin/users/{email}/check returns response without auth (no 401)
- CORS uses explicit origin list from config, not wildcard
- Dev-mode bypass returns synthetic user when Firebase not configured
- SSE endpoint accepts ?token= query parameter
- Test suite passes with `pytest tests/ -v`
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-enforcement-and-cors-lockdown/01-01-SUMMARY.md`
</output>
