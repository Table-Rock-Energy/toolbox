---
phase: proration-fetch-missing
task: 3
total_tasks: 5
status: in_progress
last_updated: 2026-03-10T18:49:40Z
branch: debug/proration-county-download
---

<current_state>
Implementing on-demand RRC data fetching for the Proration tool. Backend endpoint and frontend UI changes are coded. Several bugs were fixed mid-session. Need to restart `make dev` and retest the "Fetch Missing Data" button.
</current_state>

<completed_work>

- Backend: Added `POST /api/proration/rrc/fetch-missing` endpoint (proration.py) — takes unmatched rows, looks them up in Firestore, returns updated rows with recalculated NRA
- Backend: Added `FetchMissingRequest` and `FetchMissingResult` Pydantic models (models/proration.py)
- Backend: Fixed `get_counties_status` async generator bug in firestore_service.py — `db.get_all()` returns async generator, changed `await` to `async for`
- Backend: Fixed `get_active_rrc_download` crash — wrapped missing Firestore composite index error in try/except
- Backend: Removed county download attempt from fetch-missing (RRC server is completely unresponsive, confirmed with 456s timeout test)
- Frontend: Simplified RRC status banner — removed download button, now informational only showing record count + last updated date
- Frontend: Added "Fetch Missing Data" orange banner with button above results table when rows are missing RRC data
- Frontend: Sorted unmatched rows (no rrc_acres) to top of table
- Frontend: Stats now compute Matched/Missing from actual row data, renamed "Failed" to "Missing RRC"
- All TypeScript + Python syntax + ruff lint checks pass
</completed_work>

<remaining_work>

- Task 3: Restart `make dev` and retest the Fetch Missing Data button — verify it hits Firestore and returns updated rows
- Task 4: If Firestore lookups still miss rows, consider adding individual RRC web queries as fallback (browser-like requests with proper headers)
- Task 5: Clean up unused state variables (isDownloadingRRC, rrcSyncJob, handleDownloadRRC, startPolling, stopPolling, rrcPollRef, STEP_LABELS) — these are orphaned after removing the download UI
</remaining_work>

<decisions_made>

- Removed the bulk RRC download button entirely — user doesn't want monthly bulk downloads exposed in UI
- Strategy is: February bulk data in Firestore is the baseline, then fetch missing data on-demand per file
- fetch-missing goes straight to Firestore lookups (no county download attempt) since RRC server is completely down
- User wants browser-like RRC access if we need to hit RRC directly (to avoid being blocked)
- Stats are computed from actual row data on frontend, not backend numbers (more accurate after fetch-missing merges)
</decisions_made>

<blockers>

- RRC website (webapps2.rrc.texas.gov) is completely unresponsive — all download attempts timeout at 120s+ even for tiny counties (Loving County). County-level and bulk downloads are not viable right now.
- Firestore composite index missing for `rrc_sync_jobs` collection (status + started_at) — causes 500 on `/rrc/download/active`. Wrapped in try/except as workaround. Could create the index via the Firebase console link in the error if needed later.
</blockers>

<context>
The Proration tool uses RRC (Railroad Commission of Texas) data to look up lease acres for mineral holder calculations. Originally did individual queries, then moved to bulk file downloads, but RRC server became too slow. Current approach: February's bulk data lives in Firestore, new data fetched per-county on demand during CSV upload. Since RRC is currently down, the fetch-missing button just re-queries Firestore (which has the Feb data). The user confirmed Feb data exists in Firestore. Key files: backend/app/api/proration.py, backend/app/services/proration/rrc_county_download_service.py, backend/app/services/firestore_service.py, frontend/src/pages/Proration.tsx.
</context>

<next_action>
Start with: Restart `make dev`, navigate to Proration, load a previous job or upload a CSV, verify the stats show all 4 numbers (Total, Processed, Matched, Missing), click "Fetch Missing Data" and confirm it returns updated rows from Firestore. Then clean up orphaned download-related state/UI code from Proration.tsx.
</next_action>
