---
phase: 03-backend-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_auth_enforcement.py
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "Every protected route returns 401 without a valid token"
    - "Every protected route returns non-401 with a valid token"
    - "GHL per-endpoint auth routes return 401 without token"
    - "Admin write routes return 401 without token"
    - "Intentionally unprotected endpoints remain accessible without auth"
  artifacts:
    - path: "backend/tests/test_auth_enforcement.py"
      provides: "Complete auth smoke tests for all protected routes"
      min_lines: 120
    - path: "backend/tests/conftest.py"
      provides: "Auth mocking fixtures (already exists, unchanged)"
  key_links:
    - from: "backend/tests/test_auth_enforcement.py"
      to: "backend/app/main.py"
      via: "router-level auth dependencies"
      pattern: "unauthenticated_client.*401"
    - from: "backend/tests/test_auth_enforcement.py"
      to: "backend/app/api/ghl.py"
      via: "per-endpoint Depends(require_auth)"
      pattern: "ghl.*401"
---

<objective>
Expand auth smoke tests to cover ALL protected routes, including GHL per-endpoint auth and admin write routes.

Purpose: TEST-02 requires every protected route has a smoke test. Currently only 9 of ~40+ endpoints are tested. This plan fills the gap by adding tests for GHL per-endpoint auth routes, admin protected routes, and authenticated success tests for every router.

Output: Expanded `test_auth_enforcement.py` with complete auth coverage. TEST-01 verified (existing infrastructure works).
</objective>

<execution_context>
@/Users/yojimbo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yojimbo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-backend-test-suite/03-RESEARCH.md

@backend/tests/conftest.py
@backend/tests/test_auth_enforcement.py

<interfaces>
<!-- Existing auth infrastructure the executor uses directly -->

From backend/tests/conftest.py:
```python
@pytest.fixture
def mock_user() -> dict:
    return {"email": "test@example.com", "uid": "test-uid"}

@pytest_asyncio.fixture
async def authenticated_client(mock_user: dict):
    # Returns AsyncClient with require_auth overridden

@pytest_asyncio.fixture
async def unauthenticated_client():
    # Returns AsyncClient with no auth overrides
```

From backend/app/core/auth.py:
```python
async def require_auth(authorization: str | None = Header(None, alias="Authorization")) -> dict:
    # Returns user dict with email, uid
async def require_admin(user: dict = Depends(require_auth)) -> dict:
    # Returns user dict, raises 403 if not admin
```

Auth coverage map (from RESEARCH.md):

Router-level auth (all endpoints protected):
- /api/extract/* -- test via GET /api/extract/health
- /api/title/* -- test via GET /api/title/health
- /api/proration/* -- test via GET /api/proration/rrc/status
- /api/revenue/* -- test via POST /api/revenue/upload
- /api/ghl-prep/* -- test via POST /api/ghl-prep/upload
- /api/history/* -- test via GET /api/history/jobs
- /api/ai/* -- test via GET /api/ai/status
- /api/enrichment/* -- test via GET /api/enrichment/status
- /api/etl/* -- test via GET /api/etl/health

Per-endpoint auth (need individual testing):
- /api/ghl/connections GET -- Depends(require_auth)
- /api/ghl/contacts/upsert POST -- Depends(require_auth)
- /api/ghl/contacts/bulk-send POST -- Depends(require_auth)
- /api/ghl/send/{job_id}/cancel POST -- Depends(require_auth)
- /api/ghl/send/{job_id}/status GET -- Depends(require_auth)
- /api/ghl/connections/{id}/quick-check POST -- Depends(require_auth)
- /api/admin/users POST -- Depends(require_admin)
- /api/admin/users/{email} PUT/DELETE -- Depends(require_admin)

Intentionally unprotected:
- /api/health
- /api/admin/users/{email}/check
- /api/admin/options
- /api/admin/settings/* GET
- /api/ghl/daily-limit
- /api/ghl/send/{job_id}/progress (SSE, query-param token)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand auth smoke tests for full route coverage</name>
  <files>backend/tests/test_auth_enforcement.py</files>
  <action>
Rewrite `backend/tests/test_auth_enforcement.py` to provide complete auth smoke test coverage. Keep ALL existing tests (they are proven working). Add the following new tests:

**GHL per-endpoint auth (unauthenticated = 401):**
- `test_unauthenticated_ghl_connections_returns_401` -- GET /api/ghl/connections
- `test_unauthenticated_ghl_contacts_upsert_returns_401` -- POST /api/ghl/contacts/upsert
- `test_unauthenticated_ghl_bulk_send_returns_401` -- POST /api/ghl/contacts/bulk-send
- `test_unauthenticated_ghl_cancel_returns_401` -- POST /api/ghl/send/test-job/cancel
- `test_unauthenticated_ghl_status_returns_401` -- GET /api/ghl/send/test-job/status
- `test_unauthenticated_ghl_quick_check_returns_401` -- POST /api/ghl/connections/test-id/quick-check

**Admin write endpoints (unauthenticated = 401):**
- `test_unauthenticated_admin_create_user_returns_401` -- POST /api/admin/users
- `test_unauthenticated_admin_update_user_returns_401` -- PUT /api/admin/users/test@example.com
- `test_unauthenticated_admin_delete_user_returns_401` -- DELETE /api/admin/users/test@example.com

**Authenticated success tests (non-401) for each router not already tested:**
- `test_authenticated_title_succeeds` -- GET /api/title/health
- `test_authenticated_proration_succeeds` -- GET /api/proration/rrc/status
- `test_authenticated_history_succeeds` -- GET /api/history/jobs
- `test_authenticated_etl_succeeds` -- GET /api/etl/health
- `test_authenticated_ghl_connections_succeeds` -- GET /api/ghl/connections

**Intentionally unprotected endpoints (verify accessible):**
- `test_ghl_daily_limit_no_auth_required` -- GET /api/ghl/daily-limit (expect != 401)
- `test_admin_options_no_auth_required` -- GET /api/admin/options (expect != 401)

Add a module-level docstring comment documenting that `/api/ghl/send/{job_id}/progress` uses query-param token auth and is intentionally excluded from standard smoke tests.

IMPORTANT: For all authenticated tests, assert `response.status_code != 401` (NOT `== 200`). Many endpoints will return 500 or 422 because Firestore/GCS is unavailable in test -- that's fine. We're only testing the auth gate.

For admin write endpoints, note that `require_admin` checks both auth AND admin role. An authenticated non-admin user will get 403 (not 401). Test unauthenticated = 401. For the admin-specific check, optionally add a test showing authenticated non-admin gets 403 on POST /api/admin/users.
  </action>
  <verify>
    <automated>cd /Users/yojimbo/Documents/dev/toolbox/backend && python3 -m pytest tests/test_auth_enforcement.py -v</automated>
  </verify>
  <done>All protected routes have 401 smoke tests. All router-level and per-endpoint auth routes covered. Authenticated success tests confirm auth gate passes. Unprotected endpoints verified accessible. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Verify existing test infrastructure works (TEST-01 validation)</name>
  <files>backend/tests/conftest.py</files>
  <action>
Run the full existing test suite to confirm TEST-01 is already satisfied. The conftest.py fixtures (`authenticated_client`, `unauthenticated_client`, `mock_user`) using `app.dependency_overrides[require_auth]` are already implemented and working.

Do NOT modify conftest.py unless a test from Task 1 reveals an issue (e.g., need `require_admin` override for admin tests). If admin write endpoints need an admin-capable fixture, add a `mock_admin_user` fixture that returns `{"email": "james@tablerocktx.com", "uid": "admin-uid"}` and an `admin_client` fixture that overrides both `require_auth` and `require_admin`.

Verify: run `python3 -m pytest -v` and confirm all existing + new tests pass (no Firebase tokens, no GCP credentials needed).
  </action>
  <verify>
    <automated>cd /Users/yojimbo/Documents/dev/toolbox/backend && python3 -m pytest -v</automated>
  </verify>
  <done>Full test suite passes. conftest.py fixtures work for all auth patterns (router-level and per-endpoint). No real Firebase tokens or GCP credentials needed. TEST-01 confirmed satisfied.</done>
</task>

</tasks>

<verification>
- `cd backend && python3 -m pytest tests/test_auth_enforcement.py -v` -- all auth smoke tests pass
- `cd backend && python3 -m pytest -v` -- full suite passes (includes existing CORS tests)
- Test count: should be 30+ tests (original 14 + ~16 new auth tests)
</verification>

<success_criteria>
- Every protected route (router-level and per-endpoint) has a 401 unauthenticated test
- Every router has at least one authenticated success test (non-401)
- Intentionally unprotected endpoints verified accessible
- GHL SSE progress endpoint documented as excluded
- All tests pass without Firebase tokens or GCP credentials
</success_criteria>

<output>
After completion, create `.planning/phases/03-backend-test-suite/03-01-SUMMARY.md`
</output>
