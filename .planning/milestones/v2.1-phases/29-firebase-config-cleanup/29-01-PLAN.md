---
phase: 29-firebase-config-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - backend/app/core/config.py
  - backend/app/core/auth.py
  - backend/app/api/admin.py
  - backend/scripts/create_admin.py
autonomous: true
requirements: [CLEAN-02, CLEAN-03]

must_haves:
  truths:
    - "Dockerfile has zero VITE_FIREBASE_* ARG or ENV references"
    - "No hardcoded james@tablerocktx.com in auth.py or admin.py"
    - "App starts correctly when DEFAULT_ADMIN_EMAIL is unset (falls back to james@tablerocktx.com)"
  artifacts:
    - path: "Dockerfile"
      provides: "Clean Dockerfile without dead Firebase ARGs"
      contains: "npm run build"
    - path: "backend/app/core/config.py"
      provides: "default_admin_email setting"
      contains: "default_admin_email"
    - path: "backend/app/core/auth.py"
      provides: "Admin email from settings instead of hardcoded"
      contains: "settings.default_admin_email"
    - path: "backend/app/api/admin.py"
      provides: "Admin email from settings instead of hardcoded"
      contains: "settings.default_admin_email"
  key_links:
    - from: "backend/app/core/auth.py"
      to: "backend/app/core/config.py"
      via: "settings.default_admin_email"
      pattern: "settings\\.default_admin_email"
    - from: "backend/app/api/admin.py"
      to: "backend/app/core/config.py"
      via: "settings.default_admin_email"
      pattern: "settings\\.default_admin_email"
---

<objective>
Remove dead Firebase build args from Dockerfile and extract hardcoded admin email to a configurable env var.

Purpose: Clean up leftover Firebase references from v2.0 migration and make admin email configurable without code changes.
Output: Clean Dockerfile, configurable DEFAULT_ADMIN_EMAIL with fallback default.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Firebase ARGs from Dockerfile (CLEAN-02)</name>
  <files>Dockerfile</files>
  <read_first>Dockerfile</read_first>
  <action>
Remove the 7 VITE_FIREBASE_* ARG declarations (lines 10-16) and the entire RUN block that passes them as env vars to npm run build (lines 28-35). Replace with a simple `RUN npm run build` command.

Specifically delete these lines:
- `ARG VITE_FIREBASE_API_KEY=AIzaSyDGTk6hpc4dk2MPGCBoky_kegUrg7dUuYk`
- `ARG VITE_FIREBASE_AUTH_DOMAIN=tablerockenergy.firebaseapp.com`
- `ARG VITE_FIREBASE_PROJECT_ID=tablerockenergy`
- `ARG VITE_FIREBASE_STORAGE_BUCKET=tablerockenergy.firebasestorage.app`
- `ARG VITE_FIREBASE_MESSAGING_SENDER_ID=781074525174`
- `ARG VITE_FIREBASE_APP_ID=1:781074525174:web:f00b83c5401fe4b00d35d7`
- `ARG VITE_FIREBASE_MEASUREMENT_ID=G-YZYXTHXBV9`
- The comment `# Firebase config (public client-side values, not secrets)`
- The multi-line `RUN VITE_FIREBASE_API_KEY=... npm run build` block

Replace the build block with:
```
# Build production bundle
RUN npm run build
```

Also delete the stale comment `# Build production bundle (Vite picks up ARGs as env vars)` since there are no more ARGs.
  </action>
  <verify>
    <automated>grep -c "VITE_FIREBASE" /Users/yojimbo/Documents/dev/toolbox/Dockerfile | grep -q "^0$" && echo "PASS: No VITE_FIREBASE references" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c VITE_FIREBASE Dockerfile` returns 0
- `grep "npm run build" Dockerfile` returns exactly one line: `RUN npm run build`
- No ARG lines referencing Firebase remain
- Dockerfile still has both stages (frontend-builder and python:3.11-slim)
  </acceptance_criteria>
  <done>Dockerfile contains zero VITE_FIREBASE_* references. Build command is a simple `RUN npm run build`.</done>
</task>

<task type="auto">
  <name>Task 2: Extract admin email to config setting (CLEAN-03)</name>
  <files>backend/app/core/config.py, backend/app/core/auth.py, backend/app/api/admin.py, backend/scripts/create_admin.py</files>
  <read_first>backend/app/core/config.py, backend/app/core/auth.py, backend/app/api/admin.py, backend/scripts/create_admin.py</read_first>
  <action>
**1. Add setting to config.py:**

Add this field to the Settings class, after the `cors_allowed_origins` field (around line 83):

```python
# Default admin email (fallback when no admin exists in DB)
default_admin_email: str = "james@tablerocktx.com"
```

This maps to env var `DEFAULT_ADMIN_EMAIL` automatically via Pydantic Settings. When unset, falls back to `james@tablerocktx.com`.

**2. Update auth.py:**

Line 24 — Change the module-level constant:
```python
# Before:
DEFAULT_ADMIN_EMAIL = "james@tablerocktx.com"
# After: remove this line entirely
```

Line 190 (`is_user_admin`) — Replace hardcoded email with settings:
```python
# Before:
return email.lower() == DEFAULT_ADMIN_EMAIL
# After:
return email.lower() == settings.default_admin_email
```

Line 327 (`require_admin`) — Replace hardcoded email with settings:
```python
# Before:
if user.get("role") == "admin" or user.get("email", "").lower() == "james@tablerocktx.com":
# After:
if user.get("role") == "admin" or user.get("email", "").lower() == settings.default_admin_email:
```

**3. Update admin.py:**

Line 396 (`remove_user`) — Replace hardcoded email with settings:
```python
# Before:
if email.lower() == "james@tablerocktx.com":
# After:
from app.core.config import settings
if email.lower() == settings.default_admin_email:
```

Note: `settings` is already imported in other functions in admin.py (e.g., line 674), but this function doesn't have it in scope. Add the import inside the function or at the top of the file. Since the file already imports from `app.core.auth` at the top, add `from app.core.config import settings as app_settings` at the top-level imports (near line 30) and use `app_settings.default_admin_email` to avoid shadowing the local `settings` variable name used elsewhere. Actually, check — the file uses `runtime_settings` alias in functions. Use `from app.core.config import settings` at the top of the file (it's not currently imported at module level). Then use `settings.default_admin_email` on line 396.

**4. Update create_admin.py:**

This script has 4 references to `james@tablerocktx.com`. Update it to import from config:

Add near the top (after existing imports):
```python
from app.core.config import settings
```

Replace all 4 hardcoded email references with `settings.default_admin_email`:
- Line 33: `.where(User.email == settings.default_admin_email)`
- Line 42: `print(f"Admin user password updated: {settings.default_admin_email}")`
- Line 45: `email=settings.default_admin_email,`
- Line 54: `print(f"Admin user created: {settings.default_admin_email}")`

**5. Do NOT change test files** (conftest.py, test_auth.py). Test fixtures use hardcoded emails intentionally as test data — they should match the default value but aren't "configuration" that needs extracting.
  </action>
  <verify>
    <automated>grep -rn "james@tablerocktx.com" /Users/yojimbo/Documents/dev/toolbox/backend/app/ | grep -v __pycache__ | grep -v ".pyc" && echo "FAIL: hardcoded email still in app/" || echo "PASS: no hardcoded email in app/"</automated>
  </verify>
  <acceptance_criteria>
- `grep -rn "james@tablerocktx.com" backend/app/core/auth.py` returns zero matches
- `grep -rn "james@tablerocktx.com" backend/app/api/admin.py` returns zero matches
- `grep -rn "james@tablerocktx.com" backend/scripts/create_admin.py` returns zero matches
- `grep "default_admin_email" backend/app/core/config.py` returns a line with `default_admin_email: str = "james@tablerocktx.com"`
- `grep "settings.default_admin_email" backend/app/core/auth.py` returns at least 2 matches (is_user_admin + require_admin)
- `grep "settings.default_admin_email" backend/app/api/admin.py` returns at least 1 match (remove_user)
- Test files (backend/tests/) are unchanged
- `python3 -c "from app.core.config import Settings; s = Settings(); assert s.default_admin_email == 'james@tablerocktx.com'"` succeeds (run from backend/)
  </acceptance_criteria>
  <done>All hardcoded admin email references in backend/app/ and backend/scripts/ replaced with settings.default_admin_email. Config falls back to james@tablerocktx.com when DEFAULT_ADMIN_EMAIL env var is unset.</done>
</task>

</tasks>

<verification>
1. `grep -c VITE_FIREBASE Dockerfile` returns 0
2. `grep -rn "james@tablerocktx.com" backend/app/` returns 0 matches (excluding __pycache__)
3. `cd backend && python3 -c "from app.core.config import Settings; s = Settings(); print(s.default_admin_email)"` prints `james@tablerocktx.com`
4. `cd backend && python3 -m py_compile app/core/auth.py && python3 -m py_compile app/api/admin.py && python3 -m py_compile app/core/config.py` all succeed
5. `make test` passes (existing tests unaffected)
</verification>

<success_criteria>
- Dockerfile has zero VITE_FIREBASE_* references
- Backend app/ has zero hardcoded james@tablerocktx.com references
- DEFAULT_ADMIN_EMAIL env var is configurable with correct fallback default
- All existing tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/29-firebase-config-cleanup/29-01-SUMMARY.md`
</output>
