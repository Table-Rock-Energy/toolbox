---
phase: 16-revenue-multi-pdf-streaming
plan: 02
type: execute
wave: 1
depends_on: ["16-01"]
files_modified: ["frontend/src/pages/Revenue.tsx"]
autonomous: true
gap_closure: true
requirements: [REV-01]

must_haves:
  truths:
    - "Expanded panel view accepts multiple PDF files"
    - "Expanded panel view shows per-file streaming progress with file name and count"
  artifacts:
    - path: "frontend/src/pages/Revenue.tsx"
      provides: "Expanded panel FileUpload with multiple=true and streamProgress indicator"
      contains: "multiple={true}"
  key_links:
    - from: "frontend/src/pages/Revenue.tsx (expanded view)"
      to: "streamProgress state"
      via: "same progress indicator block used in collapsed view"
      pattern: "streamProgress\\.index.*streamProgress\\.total"
---

<objective>
Fix the expanded panel view in Revenue.tsx to support multi-file upload and show per-file streaming progress, matching the collapsed view behavior.

Purpose: Close the single verification gap — expanded panel view currently uses `multiple={false}` and a generic spinner instead of the per-file progress indicator.
Output: Revenue.tsx with both panel views fully supporting multi-PDF streaming progress.
</objective>

<execution_context>
@/Users/yojimbo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yojimbo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-revenue-multi-pdf-streaming/16-01-SUMMARY.md
@.planning/phases/16-revenue-multi-pdf-streaming/16-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix expanded panel view multi-file upload and progress indicator</name>
  <files>frontend/src/pages/Revenue.tsx</files>
  <read_first>
    - frontend/src/pages/Revenue.tsx (lines 620-655 for collapsed view reference, lines 658-676 for expanded view to fix)
  </read_first>
  <action>
In the expanded panel view (the `!panelCollapsed` block starting around line 660):

1. Change `multiple={false}` to `multiple={true}` at line 666
2. Change `label="Upload Revenue Statement"` to `label="Upload Revenue Statements"` at line 667
3. Change `description="Drop your PDF file here"` to `description="Drop your PDF files here"` at line 668
4. Replace the generic spinner block (lines 670-675) with the same streamProgress indicator used in the collapsed view. Specifically, replace:
```tsx
{isProcessing && (
  <div className="mt-4 flex items-center gap-2 text-tre-teal">
    <div className="animate-spin rounded-full h-4 w-4 border-b-2 border-tre-teal"></div>
    <span className="text-sm">Processing...</span>
  </div>
)}
```
With the two-block pattern from the collapsed view:
```tsx
{isProcessing && streamProgress && streamProgress.total > 0 && (
  <div className="mt-4 space-y-2">
    <div className="flex items-center gap-2 text-tre-teal">
      <div className="animate-spin rounded-full h-4 w-4 border-b-2 border-tre-teal"></div>
      <span className="text-sm font-medium">
        {streamProgress.status === 'post-processing'
          ? 'Finalizing results...'
          : `Processing ${streamProgress.index} of ${streamProgress.total}: ${streamProgress.file}`}
      </span>
    </div>
    <div className="w-full bg-gray-200 rounded-full h-1.5">
      <div
        className="bg-tre-teal h-1.5 rounded-full transition-all duration-300"
        style={{ width: `${(streamProgress.index / streamProgress.total) * 100}%` }}
      />
    </div>
  </div>
)}
{isProcessing && !streamProgress && (
  <div className="mt-4 flex items-center gap-2 text-tre-teal">
    <div className="animate-spin rounded-full h-4 w-4 border-b-2 border-tre-teal"></div>
    <span className="text-sm">Processing...</span>
  </div>
)}
```

This is a direct copy of the collapsed view's progress block. No new logic needed.
  </action>
  <verify>
    <automated>cd /Users/yojimbo/Documents/dev/toolbox && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep: `multiple={true}` appears TWICE in Revenue.tsx (once for collapsed view, once for expanded view)
    - grep: `Upload Revenue Statements` appears TWICE in Revenue.tsx
    - grep: `streamProgress.index` appears at least TWICE in Revenue.tsx (both views)
    - grep: `multiple={false}` does NOT appear in Revenue.tsx
    - grep: no standalone `Processing...</span>` without streamProgress fallback context
  </acceptance_criteria>
  <done>Both collapsed and expanded panel views in Revenue.tsx accept multiple PDF files and show identical per-file streaming progress with file name, count, and progress bar.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` compiles without errors
2. `grep -c 'multiple={true}' frontend/src/pages/Revenue.tsx` returns 2
3. `grep -c 'streamProgress.index' frontend/src/pages/Revenue.tsx` returns at least 2
4. `grep -c 'multiple={false}' frontend/src/pages/Revenue.tsx` returns 0
</verification>

<success_criteria>
- Expanded panel view accepts multiple PDFs (multiple={true})
- Expanded panel view shows "Processing N of M: filename.pdf" with progress bar during streaming upload
- Expanded panel view falls back to generic "Processing..." spinner when streamProgress is null
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/16-revenue-multi-pdf-streaming/16-02-SUMMARY.md`
</output>
