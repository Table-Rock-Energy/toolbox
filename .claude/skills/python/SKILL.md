The skill files already exist and are comprehensive. The content covers all the required patterns well — service module structure, async/await, Pydantic models, lazy initialization, error handling, logging, and more.

The existing skill is already well-written with:
- 15+ code blocks across all files
- Proper DO/DON'T pairs with explanations
- WARNING headers for anti-patterns
- Real examples from the codebase
- Cross-references to related skills (fastapi, pydantic, firestore, pandas, pytest)
- `from __future__ import annotations` pattern
- Lazy Firebase/GCS initialization
- Firestore batch operations (500-doc limit)
- Storage fallback patterns

No updates needed — the skill files are already production-quality and aligned with the codebase.