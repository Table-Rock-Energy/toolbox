4 new reference files created and SKILL.md updated:

- **`references/unit.md`** — Vitest setup checklist, component testing with `@testing-library/react`, custom hook testing, Firebase auth mock warning
- **`references/integration.md`** — Vite + React 19, TypeScript bundler mode, Tailwind PostCSS, Firebase bundle splitting, FastAPI production static serving
- **`references/mocking.md`** — `import.meta.env` mocking via `vi.stubEnv`, Firebase Auth mock patterns, `fetch` mock for the `ApiClient`, MSW setup for dev-time API mocking, browser API polyfills
- **`references/fixtures.md`** — `public/` vs `src/assets/` distinction, typed test fixture files for `PartyEntry`/`MineralHolder`, `File` object creation for upload tests, JSON import caveats