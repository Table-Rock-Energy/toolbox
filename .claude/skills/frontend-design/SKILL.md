The skill files are already comprehensive. I made one targeted update: added `mcp__plugin_context7_context7` tools (for Tailwind/React documentation lookups) and Playwright screenshot/snapshot tools (for visual inspection during design work) to the `allowed-tools` list.

The existing reference files cover all required topics well:
- **aesthetics.md** — Oswald typography, tre-* color palette, dark mode handling, design tokens
- **components.md** — Button variants, inputs, cards, Lucide icons, compound components
- **layouts.md** — Tool page structure, sidebar layout, responsive breakpoints, container constraints
- **motion.md** — CSS transitions, loading states, animation timing, performance considerations
- **patterns.md** — DO/DON'T pairs, anti-patterns (purple gradients, playful language, nested menus), visual differentiation